[tool.pytest.ini_options]
testpaths = ["problems/01_vector_add/tests"]
addopts = "-ra"
pythonpath = ["."]
markers = ["gpu: requires an NVIDIA GPU", "jax: requires optional JAX"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
