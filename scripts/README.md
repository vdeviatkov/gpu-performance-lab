# Future entry points

No active setup, benchmark, profiling, or build scripts are supplied at this
planning stage. Introduce small commands when there is a real operation to run.

Expected future responsibilities are environment reporting, correctness checks,
benchmark invocation, and focused Nsight capture after compilation/warmup.
Commands should make timing scope and output locations explicit. Avoid wrapping
large frameworks or adding Docker before a concrete reproducibility need exists.

Previous scripts remain preserved in the [archive](../archive/README.md).
